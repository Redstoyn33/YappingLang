Def-fn "Определим-функцию (Def-fn);
Def-fn "Определим (Def-fn);
Def-fn "Число! (Integer!);
Def-fn "Если (If);
Def-fn "Или (Or);


Определим-функцию "Сумма-делимых (
    Определим "До как Число! до которого мы вычисляем сумму;
    Определим-функцию "Делим? как (
        Определим "Число;
        Если Или == 0 % Число 3 == 0 % Число 5 (Число) (0);
    );

);